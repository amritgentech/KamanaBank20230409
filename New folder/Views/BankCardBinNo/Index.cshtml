@model IEnumerable<Db.Model.BankCardBinNo>

@{
    ViewBag.Title = "Index";
    ViewBag.MenuTitle = "Bin No";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BinNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NetworkType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BinNo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NetworkType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BankCardBinNoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BankCardBinNoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BankCardBinNoId })
        </td>
    </tr>
}

</table>
